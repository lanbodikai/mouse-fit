"use client";

interface ShellNavProps {
  currentPage: string;
}

export function ShellNav(_props: ShellNavProps) {
  // Deprecated: Replaced by DashboardShell (Sidebar + TopBar)
  void _props;
  return null;
}
